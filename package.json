{
  "name": "dgui",
  "version": "0.0.3",
  "description": "The ultimate GUI to become",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "server": "nodemon --watch server server/index.js",
    "build": "rollup -c",
    "release": "npm run build && minify build/index.js -o build/index.js",
    "bump": "npm version patch && npm publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Damien Mortini",
  "license": "MIT",
  "dependencies": {
    "@webcomponents/webcomponentsjs": "^2.0.2",
    "dlib": "0.0.81",
    "nodemon": "^1.17.5",
    "ws": "^5.2.1"
  },
  "devDependencies": {
    "babel-minify": "^0.4.3",
    "browser-sync": "^2.24.5",
    "http2": "^3.3.7",
    "rollup": "^0.62.0"
  }
}
